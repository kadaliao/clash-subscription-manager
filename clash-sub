#!/bin/bash
# Clash 订阅管理器启动脚本

# 设置 PATH，确保能找到 uv
export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"

# 获取脚本所在目录
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 切换到项目目录并运行
cd "$SCRIPT_DIR" && uv run python clash_sub_manager.py "$@"
