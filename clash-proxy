#!/bin/bash
# Clash 代理节点选择器启动脚本

# 设置 PATH，确保能找到 uv
export PATH="/opt/homebrew/bin:/usr/local/bin:$PATH"

# 获取脚本所在目录
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# 切换到项目目录并运行
# Python 脚本会自动读取 .clash-api-config 配置文件
cd "$SCRIPT_DIR" && uv run python clash_proxy_selector.py "$@"
